import pandas as pd


def seperate_df(cleaned_df: pd.DataFrame):

    separated_dfs = {}
    # for value in cleaned_df["some_column"].unique():
    #     separated_dfs[value] = cleaned_df[cleaned_df["some_column"] == value]
    return separated_dfs
